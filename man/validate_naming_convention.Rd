% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_naming_convention.R
\name{validate_naming_convention}
\alias{validate_naming_convention}
\title{Validate and annotate a dataset using a naming convention}
\usage{
validate_naming_convention(df, naming_object)
}
\arguments{
\item{df}{A data frame to validate.}

\item{naming_object}{A data frame with columns:
\itemize{
\item \code{name} (character): column name in \code{df}
\item \code{mandatory} (logical): whether the column is required
\item \code{valid_values} (list-column): allowed values for each variable (or NULL for no check)
\item \code{default_description} (character): description to store as a column attribute
}}
}
\value{
The annotated data frame.
}
\description{
Adds description attributes to columns and checks for missing mandatory variables and invalid values.
}
\examples{
# Create dataset
df1 <- data.frame(
  Site_ID = rep(c("BLA", "BRA", "GUL", "HAL", "HEM", "LAN", "MRK", "OYT", "SAR", "SKO", "STR", "TRE"), each = 2),
  Forest_Type = rep(c("CC", "NN"), times = 12)) \%>\% 
  mutate(Plot_ID = paste(Site_ID, Forest_Type, sep = "_"))

data(EcoForest_naming)

# Check if all mandatory variables are included with only valid entries.
df1 <- validate_naming_convention(df1, EcoForest_naming)

#Check attribute added to Site_ID 
attributes(df1$Site_ID)$description
}
